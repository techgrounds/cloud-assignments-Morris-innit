# A list consisting of 5 names.
listNames = ['Shikha', 'Jaz', 'Sam', 'Elmarie', 'Casper']

# A for loop printing each element of the list using indexing.
for i in range(len(listNames)):
    print(listNames [i])
